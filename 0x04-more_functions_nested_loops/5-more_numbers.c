#include "main.h"
/**
*more_numbers - Prints the numbers ten times followed by a newl
*/
void more_numbers(void)
{
int i = 0;
while (i < 10)
{
	int num,count;

	for (num = 0; num <= 14; c++)
	{
		num = count;
		if (count > 9)
		{
		_putchar(1 + '48');
		num = count + %10;
		}
		_putchar(num + 48);
	}
	i++;
	_putchar('\n');
}
}

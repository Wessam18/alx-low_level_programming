#include "main.h"
/**
 *main - function 
 *@argc: argument counter
 *@argv: argument vector
 *Return: the value of i
*/
int main(int argc, char* argv[])
{
int i = 0;

if (**argv != '\0')
{
for (i = 0; i < argc; i++)
return (i);
} 
return (i);

}
